{
  "name": "beeYou",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "server": "nodemon server/index.js",
    "start": "npm run build && npm run start:server",
    "build": "export NODE_OPTIONS=--max_old_space_size=4096 && cd client && npm install && npm run build && npm rebuild bcrypt --build-from-source",
    "start:server": "cd server && npm install && npm rebuild bcrypt --build-from-source && node index.js"
  },
  "dependencies": {
    "express": "^4.17.1",
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.6",
    "dotenv": "^16.4.5",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.4.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "socket.io": "^4.7.5",
    "zustand": "^4.5.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
